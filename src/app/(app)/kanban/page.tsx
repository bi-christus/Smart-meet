import { ModulePlaceholder } from "@/components/module-placeholder";

export default function KanbanPage() {
  return (
    <ModulePlaceholder
      icon="kanban"
      title="Kanban"
      subtitle="Quadros de demandas por setor — colunas, cards, responsáveis, prazos e checklists."
      phase="Em construção · Fase 3"
    />
  );
}
